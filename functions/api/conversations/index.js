import { id, json, now, requireSameOrigin, requireUser, run, queryAll, fail } from "../../_lib/app.js";
export async function onRequestGet({ request, env }) { const auth = await requireUser(request, env); if (auth.response) return auth.response; return json(await queryAll(env.DB, "SELECT id, title, created_at, updated_at FROM conversations WHERE owner_id = ? ORDER BY updated_at DESC", [auth.user])); }
export async function onRequestPost({ request, env }) { const auth = await requireUser(request, env); if (auth.response) return auth.response; if (!requireSameOrigin(request)) return fail("forbidden", "来源不正确", 403); const value = { id: id(), title: "新对话", created_at: now(), updated_at: now() }; await run(env.DB, "INSERT INTO conversations (id, owner_id, title, created_at, updated_at) VALUES (?, ?, ?, ?, ?)", [value.id, auth.user, value.title, value.created_at, value.updated_at]); return json(value, 201); }
